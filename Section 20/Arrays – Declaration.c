#include <stdio.h>
int main()
{
    int grade[5]; // an array with 5 elements of type integer  

    double salaries[10]; // an array with 10 elements of type double

    char myArray[100]; // an array with 100 elements of type char

    return 0;
}